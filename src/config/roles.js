
// src\config\roles.js
export const ROLES = {
  ADMIN:      "Admin",
  
};

const { ADMIN,INVENTORY } = ROLES;

// ── Role Groups ────────────────────────────────────────────────────────────────
export const ALL_ROLES        = [ADMIN];
export const ADMIN_ONLY       = [ADMIN];




